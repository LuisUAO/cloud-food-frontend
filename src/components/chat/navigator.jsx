import React from 'react';

export function ChatNavigator({name}) {
    return (
        <nav class="level">
            <div class="level-left">
              <h1 class="subtitle">Chat con {name}</h1>
            </div>

            <div class="level-right">

            </div>
        </nav>
    );
}